# AstrBot Zerochan 插件

一个用于搜索和获取 [Zerochan](https://www.zerochan.net) 动漫图片的 AstrBot 插件。

## 功能

- 🔍 按标签搜索图片
- 🖼️ 根据 ID 获取图片详情
- 📊 支持分页和数量限制
- 🎨 支持尺寸和颜色过滤

## 安装

将此插件目录放入 AstrBot 的 `plugins` 文件夹中，然后重启 AstrBot 或在管理面板中启用插件。

## 命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `/zc <标签> [页码] [数量]` | 搜索图片 | `/zc Genshin Impact` |
| `/zcid <ID>` | 根据 ID 获取详情 | `/zcid 3793685` |
| `/zchelp` | 显示帮助信息 | `/zchelp` |

## 使用示例

### 搜索图片

```
/zc Genshin Impact     # 搜索原神相关图片
/zc Lumine 1 5         # 搜索荧，第1页，显示5张
/zc Naruto, Sasuke     # 搜索多个标签
```

### 获取图片详情

```
/zcid 3793685          # 获取指定 ID 的图片详情
```

## Zerochan API 说明

本插件基于 Zerochan API 开发，具有以下限制：

- **只读 API**: 仅支持 GET 请求
- **频率限制**: 每分钟最多 60 次请求
- **User-Agent**: 需要设置有效的 User-Agent 头

### API 端点

1. **搜索所有条目**: `/?json`
2. **按标签搜索**: `/<tag>?json`
3. **多标签搜索**: `/<tag1>,<tag2>?json`
4. **获取详情**: `/<id>?json`

### 参数说明

| 参数 | 类型 | 说明 |
|------|------|------|
| `p` | int | 页码 |
| `l` | 1-250 | 每页数量 |
| `s` | id/fav | 排序方式 |
| `d` | large/huge/landscape/portrait/square | 尺寸过滤 |
| `c` | 颜色名 | 颜色过滤 |

## 依赖

- `aiohttp` - 异步 HTTP 客户端

## 开发

### 项目结构

```
astrbot_plugin_zerochan/
├── main.py           # 插件主文件
├── metadata.yaml     # 插件元数据
├── README.md         # 说明文档
├── LICENSE           # 许可证
└── .gitignore        # Git 忽略配置
```

### 扩展功能

可以通过修改 `main.py` 中的 `ZerochanAPI` 类来扩展更多 API 功能：

```python
# 添加尺寸过滤搜索
result = await self.api.search(
    tags="Genshin Impact",
    dimensions="large",  # 只搜索大图
    sort="fav"           # 按收藏数排序
)
```

## 许可证

GNU Affero General Public License v3.0

## 相关链接

- [AstrBot 文档](https://docs.astrbot.app)
- [Zerochan API 文档](https://www.zerochan.net/api)
- [GitHub 仓库](https://github.com/vmoranv/astrbot_plugin_zerochan)
